#ifndef UTILS_HPP
#define UTILS_HPP

enum AreaType {
    ZERO = 0, // size = 0
    FINITE = 1, // 0 < size < finite
    INFINITE = 2, // size = finite
};

enum DirType {
    SINGULAR = 0,
    CONTINOUS = 1,
};

#endif
